/**
 * SkillDefinition — passthrough wrapper that renders its children.
 * Placeholder for a future skill-glossary tooltip; the `skill` prop
 * is accepted for that future use but unused today.
 */
export default function SkillDefinition({ children }) {
  return children;
}
